{% extends "layout.html.tera" %}

{% block description %}Things Christian Kruse likes{% endblock %}

{% block content %}
<header class="page-header">
  <h2>Likes</h2>
  <a href="{{likes_uri()}}.atom"><img class="feed-icon" src="{{asset_uri(asset="feed.svg")}}" alt="Atom Feed"></a>
</header>

<p>This page contains things <a href="{{page_uri(page="about")}}" rel="me">I</a> found and like.</p>

{#<%= if logged_in?(@conn) do %>
  <p><%= link "New like", to: Routes.like_path(@conn, :new) %></p>
<% end %>#}

<ol class="likes">
  {% for like in likes %}
    <li>
      {% include "likes/like.html.tera" %}

      {#<%= if logged_in?(@conn) do %>
      <p class="index-actions">
      <%= link "Edit", to: Routes.like_path(@conn, :edit, like) %>
      <%= button "Delete", to: Routes.like_path(@conn, :delete, like), method: :delete, data: [confirm: "Are you sure?"] %>
      </p>
      <% end %>#}
    </li>
  {% endfor %}
</ol>

{% set paging_path = likes_uri() %}
{% include "paging.html.tera" %}
{% endblock %}
