<article
  class="note h-entry{% if index %} index{% endif %}{% if !note.show_in_index %} unpublished{% endif %}"
  {# TODO: <%= WwwtechWeb.LayoutView.entry_class_by_type(@note.note_type) %>#}
  lang="{{note.lang}}">
  {% if !atom %}
    <header>
      <h3 class="hidden p-name">{{note.title}}</h3>

      <p>
        <span class="p-author h-card">
          <a href="{{self::page_uri("about")}}" rel="me">
            <img src="{{self::asset_uri("christian-kruse.jpg")}}" class="avatar u-photo" alt="">
            <span class="p-name">Christian Kruse</span>
          </a>
        </span>
      </p>
    </header>
  {% endif %}

  {% if note.posse && !index && !atom %}
    <a href="https://brid.gy/publish/twitter" class="hidden"></a>
    <a href="https://brid.gy/publish/mastodon" class="hidden"></a>
  {% endif %}

  {% if let Some(reply) = note.in_reply_to %}
    <p class="in-reply-to">
      {% if note.note_type == "repost" %}
        A repost of
      {% else %}
        In reply to
      {% endif %}

      <a rel="in-reply-to" class="{{note.note_type|link_class_by_type}}" href="{{reply}}">{{reply}}</a>
    </p>
  {% endif %}

  <div class="e-content">
    {{note.content|markdown2html|safe}}
    <time class="perma-link dt-published" datetime="{{note.inserted_at|date_format("%FT%T")}}">
      <a href="{{self::note_uri(note)}}" class="u-url">{{note.inserted_at|time_ago_in_words}}</a>
    </time>
  </div>

  {# <%= render WwwtechWeb.LayoutView, "answers.html", Map.put(assigns, :mentions, @note.mentions) %> #}
</article>
