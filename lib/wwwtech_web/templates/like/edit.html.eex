<header class="page-header">
  <h2>Edit like <%= @like.id %></h2>
</header>

<%= render "form.html", Map.merge(assigns, %{action: Routes.like_path(@conn, :update, @like), back: Routes.like_path(@conn, :index)}) %>
